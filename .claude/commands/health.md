---
description: Audit Ignite workspace health, gaps, traceability, and readiness.
---

# Ignite Health

Arguments received from the user invocation: `$ARGUMENTS`

Parse `PROJECT_ID` from:

```text
/health PROJECT_ID
```

Run:

```powershell
python -m sentinel /health PROJECT_ID
```

Report the verdict and highest-priority findings.
