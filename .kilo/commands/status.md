---
description: Show project phase, health, language, gap counts, and next step.
agent: sentinel-health
---

# Ignite Status

Parse `PROJECT_ID` from:

```text
/status PROJECT_ID
```

Run from the repository root:

```powershell
python -m sentinel /status PROJECT_ID
```

Summarize phase, health, readiness, gap counts, and next recommended step.
