# Baseline de Estado Validado - 2026-06-10

Punto de partida verificado para la fase de mejoras y evolución de Ignite Sentinel vNext. Generado cruzando `general-proyecto.md`, `ignite_vnext_final_handoff.md` y el estado real del repositorio.

## Git

- Branch: `main`, HEAD `6b2a327 Make Sentinel portable across Codex and Kilo` (un commit posterior al handoff consolidado, que registraba `aae196a`).
- Working tree: 79 archivos aparecen modificados pero el diff es exclusivamente de line endings (CRLF/LF, +1951/-1951 simétricos). No hay cambios de contenido pendientes.
- Archivos no trackeados: `general-proyecto.md`, `ignite_vnext_final_handoff.md` (copias de handoffs en la raíz del repo) y este directorio `docs/`.
- Branches locales adicionales: `codex/portable-vscode-adapters`, `codex/slash-commands-workspace-scaffold`.
- Repo público con branch protection: cambios vía branch + PR.

## Runtime y tests

- `sentinel/` con 24 módulos, ~6.100 líneas Python. Comandos confirmados: `/doctor /init /ingest /retrieve /sync /maturity /specs /backlog /quality /health /trace /validate /reindex /gaps /resolve-gaps /brief /context-request /status /export`.
- Tests: 17 en `tests/test_core_flow.py`. En entorno con `lancedb` instalado: 17 OK (validado en el handoff). En sandbox sin `lancedb`: 15 OK y 2 fallos de doctor por dependencia faltante (`required dependency: lancedb`, `LanceDB local open/create`) — fallo de entorno, no de código.
- `pyproject.toml`: Python >=3.10, dependencia requerida `lancedb`, extra opcional `memory` (`sentence-transformers`).

## Gates de lifecycle implementados

- `/specs` y `/backlog` requieren `/ingest` previo y fallan con maturity `BLOCKED`.
- `/backlog` y `/quality` bloqueados con health `DIRTY`.
- `/quality` requiere user stories existentes.
- Comandos de proyecto fallan si el workspace no existe.
- Todo comando de proyecto pasa por command protocol (preflight/postflight + `command_protocol_log.md`).

## Adapters y documentación

- Codex: `AGENTS.md`, 12 skills en `.codex/skills/`, hooks, command router (`sentinel /COMMAND` como forma robusta).
- Kilo Code: agentes en `.kilo/agents/`, comandos en `.kilo/commands/`, `kilo.jsonc`.
- User guide: `user_guide/00` a `12` (no existe `13-chat-forensic-handoff.md`).

## Discrepancias resueltas (handoff vs. repo)

| Tema | Handoff | Estado real |
|---|---|---|
| HEAD | `aae196a` | `6b2a327` (commit posterior de portabilidad) |
| Working tree sucio | "cambios no commiteados" | Solo line endings + handoffs no trackeados |
| LanceDB | requerido por doctor | Confirmado: doctor falla sin lancedb |
| Tests | 17 OK | Confirmado (los 2 fallos en sandbox son ambientales) |

## Pendientes heredados (handoff §21, base del roadmap)

1. Validación semántica más profunda (hoy valida estructura/secciones, no completitud semántica).
2. Mejor extracción de FRs/personas/KPIs desde evidencia real, para reducir scaffolding genérico en PRD/specs.
3. Enriquecer `specs_generation.json` e `implementation_readiness.json` con scoring, coverage map y evidencia por sección.
4. Política definitiva para archivos `.7z` no trackeados y launchers.
5. Probar flujo completo desde clon limpio/ZIP en otra carpeta.
6. Mantener alineados runtime, skills, comandos, guías y tests ante cambios de lifecycle.

Además, observado en este baseline: normalizar line endings (`.gitattributes`) para eliminar el ruido de 79 archivos "modificados", y decidir si los handoffs copiados en la raíz se versionan, se mueven o se ignoran.
